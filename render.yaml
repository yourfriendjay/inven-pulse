services:
  - type: web
    name: inven-pulse
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python execution/seed_db.py && uvicorn server.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
